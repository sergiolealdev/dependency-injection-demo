import {IDistanceConverter} from '../interfaces/idistance-converter';

export class MockDistanceConverterService implements IDistanceConverter{

  convertKmToMiles (distance: number) {
    
  }

  convertMilesToKm (distance: number) {

  }

}
